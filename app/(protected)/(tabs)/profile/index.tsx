import React from "react";

import { RoutePlaceholderScreen } from "@/features/shell";

export default function ProfileRoute() {
	return (
		<RoutePlaceholderScreen
			description="User, account, former academic, preferences, and logout live here."
			subtitle="Profile, preferences, and session actions."
			title="Profile"
		/>
	);
}
