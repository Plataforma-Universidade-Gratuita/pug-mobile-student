export interface RoutePlaceholderScreenProps {
	title: string;
	subtitle?: string;
	description?: string;
	showBackButton?: boolean;
}

export interface ModalRoutePlaceholderScreenProps {
	title: string;
	subtitle?: string;
	description?: string;
}
